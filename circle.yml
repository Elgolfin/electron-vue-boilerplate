machine:
    timezone:
        America/Montreal
    node:
        version:
            6.3.0
general:
    branches:
        only:
            - master
            - electron-boilerplate-vue
test:
  post:
    - bash <(curl -s https://codecov.io/bash)
